import 'package:flutter/material.dart';

class DailyForecastWidget extends StatelessWidget {
  const DailyForecastWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('DailyForecastWidget'),
    );
  }
}
